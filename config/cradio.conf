# Mouse / pointing behaviors (&mkp, &mmv, &msc) used on layer 1
CONFIG_ZMK_POINTING=y

# ZMK Studio: live keymap editing.
# Plug the left half into your computer via USB-C, then open https://zmk.studio/
CONFIG_ZMK_STUDIO=y
